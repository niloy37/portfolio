# Favicon Notes

The portfolio uses the NR mark in `favicon.svg` and `favicon.ico`. The ICO file is kept for browsers and crawlers that still request `/favicon.ico`; the SVG is the primary high-resolution source.
