# Local environment example
#
# The current portfolio does not require local environment variables to run.
# Site metadata is configured in data/personal.ts, and the contact endpoint is
# configured in components/Contact.tsx.
NEXT_PUBLIC_SITE_URL=https://niloy37.github.io/portfolio
